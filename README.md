# IT Service Desk — тикет-система для IT-отдела клиники

Веб-приложение на PHP + MySQL для приёма и обработки заявок сотрудников
клиники в IT-отдел, плюс Telegram-бот с мини-приложением для обработки
заявок сотрудниками IT.

## Состав проекта

```
/index.php               — страница входа (ФИО + пароль)
/change_password.php     — смена пароля (обязательна при первом входе)
/dashboard.php            — создание новой заявки
/my_tickets.php           — «Мои заявки»
/all_tickets.php          — «Все заявки»
/ticket.php               — диалог по конкретной заявке
/api/                      — AJAX-обработчики веб-кабинета
/assets/                   — CSS/JS веб-кабинета
/config/config.php         — конфигурация (БД, токен бота, адрес сайта)
/includes/                 — общий PHP-код (БД, авторизация, Telegram API)
/db/schema.sql             — структура базы данных
/db/seed_employees.sql     — список сотрудников клиники (498 ФИО)
/db/seed_it_staff.sql      — назначает роль IT-отдела двум сотрудникам
                              из общего списка (выполняется после
                              seed_employees.sql)
/storage/uploads/          — загруженные файлы/фото (создаётся автоматически)
/bot/webhook.php           — приём вебхуков Telegram-бота (способ А)
/bot/setwebhook.php        — установка/снятие вебхука
/bot/poll.php              — опрос Telegram по cron, альтернатива вебхуку (способ Б)
/bot/diagnose.php          — проверка, может ли сервер достучаться до Telegram
/webapp/                   — Telegram Mini App для сотрудников IT-отдела
/webapp/tg-sdk.php         — отдаёт telegram-web-app.js со своего домена
                              (кэширует файл с telegram.org через сервер,
                              на случай если у клиента заблокирован сам
                              домен telegram.org)
/it/                       — отдельное веб-приложение (PWA) для IT-отдела —
                              то же самое, что и Mini App, но без Telegram:
                              вход по ФИО+паролю, устанавливается на главный
                              экран телефона/компьютера, push-уведомления
/it/generate_vapid_keys.php — одноразовый скрипт генерации ключей для push
/includes/webpush.php      — отправка push-уведомлений (VAPID + шифрование)
/includes/ec_math.php      — математика эллиптической кривой для Web Push
```

## 1. Требования к хостингу

Проект рассчитан на самый простой shared-хостинг:
- PHP 7.3+ (проверено на 7.3.33) с расширениями: PDO/pdo_mysql, curl, gd/fileinfo, mbstring, json — на большинстве хостингов включены по умолчанию.
- MySQL 5.7+/8.0 или MariaDB 10.3+.
- Apache с mod_rewrite/mod_headers и поддержкой `.htaccess` (AllowOverride All).
- HTTPS обязателен — без него не будут работать ни Telegram Mini App/webhook, ни push-уведомления, ни установка веб-приложения на главный экран.
- Расширение PHP **GMP** — нужно только для push-уведомлений отдельного
  веб-приложения `/it/` (раздел 3 ниже). Если его нет — весь остальной
  функционал (заявки, Telegram-бот) работает как обычно, просто без push.
  Включается в панели хостинга («Select PHP Extensions» → gmp) или через
  поддержку.

## 2. Развёртывание

### Шаг 1. Загрузите файлы
Скопируйте **всё содержимое** репозитория в корень сайта (например, `public_html`)
любым способом — FTP, файловый менеджер хостинга или git.

### Шаг 2. Создайте базу данных
В панели хостинга (cPanel и т.п.) создайте:
- базу данных, например `itdesk`;
- пользователя БД с полными правами на эту базу.

Затем через **phpMyAdmin** импортируйте по очереди **три файла в этом
порядке** (порядок важен):
1. `db/schema.sql` — создаёт таблицы;
2. `db/seed_employees.sql` — добавляет 498 сотрудников клиники;
3. `db/seed_it_staff.sql` — назначает роль IT-отдела Александру Вавилову
   и Никите Павлову (они оба уже есть в общем списке сотрудников клиники —
   этот шаг не создаёт для них отдельные записи, а помечает существующие,
   привязывая Telegram ID).

Пароль по умолчанию для **всех** сотрудников: `123456789`.
При первом входе система обязательно предложит сменить пароль или
оставить пароль по умолчанию.

### Шаг 3. Настройте config/config.php
Откройте `config/config.php` и укажите:

```php
define('DB_HOST', 'localhost');       // хост БД (обычно localhost)
define('DB_NAME', 'itdesk');          // имя базы данных
define('DB_USER', 'itdesk_user');     // пользователь БД
define('DB_PASS', 'ваш_пароль');      // пароль БД

define('APP_URL', 'https://itdesk.вашклиника.ru'); // адрес сайта БЕЗ / в конце

define('TG_WEBHOOK_SECRET', 'придумайте-случайную-строку');
```

Токен бота уже прописан (`TG_BOT_TOKEN`), его менять не нужно.

### Шаг 4. Права на папку загрузок
Убедитесь, что папка `storage/uploads/` доступна для записи веб-сервером
(обычно права `755`/`775` уже достаточно; если нет — выставьте `775`).

### Шаг 5. Подключите Telegram-бота

Сначала: оба сотрудника IT-отдела должны один раз отправить `/start` боту
(иначе Telegram не позволит боту первым написать пользователю).

Есть два способа получать сообщения от Telegram — выберите один.

#### Способ А. Вебхук (проще, но требует, чтобы хостинг умел принимать
входящие запросы от Telegram и делать исходящие HTTPS-запросы к Telegram)

1. Откройте в браузере:
   `https://ваш-сайт/bot/setwebhook.php?key=ЗНАЧЕНИЕ_TG_WEBHOOK_SECRET`
   (значение — то, что вы указали в `TG_WEBHOOK_SECRET` на шаге 3).
2. Должен появиться ответ `"ok":true` — значит, вебхук установлен.
3. После успешной установки рекомендуется удалить файл `bot/setwebhook.php` с сервера.

Если вместо ответа Telegram вы видите `null` или ошибку `curl` — сначала
откройте `https://ваш-сайт/bot/diagnose.php`, чтобы понять, может ли сервер
вообще связаться с `api.telegram.org` (частая причина на дешёвом хостинге —
блокировка исходящих HTTPS-запросов или устаревшие корневые SSL-сертификаты).
Если сервер не может связаться с Telegram, ни вебхук, ни способ Б ниже не
заработают, ни отправка уведомлений — с этим нужно разбираться с поддержкой
хостинга. Если же соединение в порядке — переходите на способ Б, он не
требует входящих запросов и проще заводится на многих хостингах.

#### Способ Б. Опрос по cron (polling) — без вебхука вообще

1. Если вебхук уже был установлен — снимите его:
   `https://ваш-сайт/bot/setwebhook.php?key=ЗНАЧЕНИЕ_TG_WEBHOOK_SECRET&remove=1`
   (Telegram не отдаёт сообщения через опрос, пока установлен вебхук.)
2. Если базу данных вы создавали ДО появления файлов
   `db/migration_1_bot_polling.sql` и `db/migration_2_notification_queue.sql`,
   выполните оба один раз через phpMyAdmin (вкладка «SQL», вставить
   содержимое файла и выполнить). При установке системы «с нуля» через
   актуальный `db/schema.sql` этот шаг не нужен — там уже всё есть.
3. В панели хостинга найдите раздел **Cron Jobs** (в cPanel — «Cron Jobs») и
   добавьте задание с интервалом **раз в минуту**, команда:
   ```
   php /home/ваш_логин/public_html/mks/bot/poll.php >/dev/null 2>&1
   ```
   (замените путь на реальный — тот, куда вы загрузили файлы сайта; узнать
   точный путь можно в файловом менеджере хостинга).
4. Проверить вручную (не обязательно, но полезно один раз): откройте
   `https://ваш-сайт/bot/poll.php?key=ЗНАЧЕНИЕ_TG_WEBHOOK_SECRET` — должно
   появиться `Обработано входящих обновлений: 0` и `Отправлено уведомлений
   из очереди: 0` (или больше, если есть что обрабатывать) без ошибок.

**Как устроены уведомления.** Веб-запросы (создание заявки, ответ,
закрытие, закрепление) никогда сами не обращаются к Telegram — они только
кладут сообщение в очередь (`notification_queue`), это мгновенная запись
в базу данных без сети. Реальную отправку делает `bot/poll.php` при
каждом запуске по cron. Так медленный или временно недоступный прокси до
Telegram никогда не блокирует и не ломает ответ сайта пользователю — в
худшем случае уведомление придёт с задержкой до минуты (до следующего
запуска cron), а не потеряется. Если отправка не удалась (прокси
недоступен), сообщение остаётся в очереди и повторяется на следующих
запусках — до 5 попыток, после чего помечается как не доставленное.

#### Если сервер вообще не может связаться с Telegram

Если `bot/diagnose.php` показывает ошибку соединения (например,
`curl_errno: 28` — таймаут, или `curl_errno: 7` — соединение отклонено) —
это не лечится ни вебхуком, ни polling: хостинг блокирует исходящие
HTTPS-запросы с сервера, а без них не работает вообще ничего, включая
отправку самих уведомлений о заявках.

1. Напишите в поддержку хостинга примерно так: «Скрипт на PHP не может
   установить исходящее HTTPS-соединение (порт 443) с внешним сервером
   api.telegram.org — таймаут через curl. Блокируете ли вы исходящие
   соединения с сервера, и можно ли это разрешить для этого домена?»
2. Если поддержка не может/не хочет снимать блокировку — можно направить
   запросы к Telegram через прокси-сервер (например, недорогой VPS в другой
   стране, который умеет ходить в Telegram). Укажите его адрес в
   `config/config.php`:
   ```php
   define('TG_PROXY', 'http://логин:пароль@адрес_прокси:порт');
   // или для SOCKS5:
   define('TG_PROXY', 'socks5://адрес_прокси:порт');
   ```
   После этого все запросы к Telegram (отправка уведомлений, polling,
   установка вебхука) автоматически пойдут через прокси. Проверить прокси
   можно тем же `bot/diagnose.php` — он покажет отдельный блок с результатом
   подключения через прокси, если `TG_PROXY` задан.

### Шаг 6. Настройте Mini App в BotFather (необязательно, но рекомендуется)
Через [@BotFather](https://t.me/BotFather) выполните `/mybots` → выберите бота →
`Bot Settings` → `Menu Button` → укажите URL: `https://ваш-сайт/webapp/`.
Тогда у сотрудников IT-отдела в чате с ботом появится кнопка быстрого
открытия панели заявок.

Готово — система полностью работает. Дальше (необязательно) — раздел 3
про отдельное веб-приложение для тех, кому неудобен Telegram.

## 3. Отдельное веб-приложение для IT-отдела (без Telegram) + push-уведомления

Помимо Telegram-бота, у IT-отдела есть полностью самостоятельное
веб-приложение — `https://ваш-сайт/it/` — с теми же разделами (Новые/Мои/
Закрытые заявки, чат, закрепление, закрытие с учётом дежурств, отчёт).
Вход по ФИО и паролю (`123456789` по умолчанию, со сменой при первом
входе — как и у обычных сотрудников). Оно устанавливается на главный
экран телефона/компьютера как отдельное приложение и умеет присылать
push-уведомления о новых заявках напрямую в браузер/на экран блокировки —
без Telegram вообще.

### Шаг 1. Применить миграцию БД (если ставили систему до появления этого раздела)
Через phpMyAdmin выполните `db/migration_3_web_push.sql` (добавляет таблицы
`push_subscriptions` и `push_queue`). При установке с нуля через актуальный
`db/schema.sql` это не нужно.

### Шаг 2. Убедиться, что на сервере есть расширение PHP GMP
Оно нужно для шифрования push-уведомлений (см. раздел 1 выше). Включите
через панель хостинга или попросите поддержку. Без него `/it/` продолжит
работать как обычно (заявки, чат) — просто без push-уведомлений.

### Шаг 3. Сгенерировать ключи VAPID
Откройте в браузере:
`https://ваш-сайт/it/generate_vapid_keys.php?key=ЗНАЧЕНИЕ_TG_WEBHOOK_SECRET`
(тот же секрет, что и для `bot/setwebhook.php`). Скрипт покажет две строки
вида:
```php
define('VAPID_PUBLIC_KEY', '...');
define('VAPID_PRIVATE_KEY_PEM', "...");
```
Скопируйте их в `config/config.php`, заменив текущие пустые значения
`VAPID_PUBLIC_KEY` и `VAPID_PRIVATE_KEY_PEM`. После этого **удалите файл**
`it/generate_vapid_keys.php` с сервера — приватный ключ больше нигде
показываться не должен.

### Шаг 4. Готово
Отправка push-уведомлений идёт по той же схеме, что и Telegram-уведомления
(раздел 2, способ Б выше) — через очередь в БД и `bot/poll.php` по cron,
без прямых обращений к сети из веб-запросов сотрудников. Отдельно ничего
настраивать не нужно, если cron для `bot/poll.php` уже работает.

Если push-сервисы (Google/Mozilla/Apple) тоже недоступны напрямую с вашего
хостинга (та же история, что с Telegram) — можно направить и их через
прокси: `define('PUSH_PROXY', 'socks5h://адрес:порт');` в `config/config.php`
(можно тот же прокси, что и `TG_PROXY`, можно другой).

### Как сотрудники IT-отдела подключают push-уведомления

**iPhone (Safari, iOS 16.4+):** открыть `https://ваш-сайт/it/` в Safari →
кнопка «Поделиться» → «На экран «Домой»» → открыть приложение **именно с
этого значка на главном экране** (не из вкладки Safari — на iOS push
работает только у установленного таким образом приложения) → при первом
входе появится баннer «Включить уведомления?» → нажать «Включить» →
подтвердить системный запрос разрешения.

**Android (Chrome):** открыть `https://ваш-сайт/it/` → Chrome сам предложит
«Добавить на главный экран» / «Установить приложение», либо через меню
(⋮) → «Установить приложение». Дальше так же — баннер «Включить
уведомления?» при входе.

**Компьютер (Chrome/Edge/Firefox):** просто открыть `https://ваш-сайт/it/`
и войти — баннер с запросом разрешения на уведомления появится
автоматически. Можно также «установить» как отдельное окно-приложение
через значок в адресной строке браузера (необязательно для уведомлений).

Если баннер закрыли не глядя — включить уведомления позже можно через
меню (⋮) в самом приложении → «Уведомления».

**Важно:** push-уведомления — это отдельный от Telegram канал. Заявка,
созданная сотрудником клиники, по-прежнему уведомляет ОБОИХ IT-сотрудников
и через Telegram, и через push (если они включили уведомления в `/it/`) —
можно пользоваться и тем, и другим одновременно, независимо друг от друга.

## 4. Как это устроено

### Сотрудники клиники (веб-кабинет)
- Вход по ФИО (с автоподбором) и паролю.
- «Создать заявку» — текст + вложения (фото/документы).
- «Мои заявки» — список своих заявок со статусами (открыта/закрыта).
- «Все заявки» — заявки всех сотрудников в режиме чтения (свои заявки
  выделены цветом), чтобы не плодить дубликаты обращений.
- Открыв заявку, сотрудник видит переписку в виде мессенджера и может
  дописывать сообщения с вложениями, пока заявка открыта.
- Если заявку закрыли, а проблема осталась — кнопка «Открыть заявку».

### IT-отдел (Telegram)
- При создании новой заявки обоим сотрудникам IT приходит сообщение
  «Новая заявка!» с кнопкой **«Забрать заявку себе»**.
- Нажатие кнопки закрепляет заявку за сотрудником и рассылает
  подтверждение обоим сотрудникам IT.
- Внутри бота доступно Mini App (кнопка «Открыть панель заявок» после `/start`,
  либо через Menu Button, либо кнопка «Открыть в приложении» в уведомлении):
  - **Новые заявки** — все открытые заявки; закреплённые помечены именем
    и цветом сотрудника (Александр — фиолетовый, Никита — голубой);
  - **Мои заявки** — заявки, закреплённые за текущим сотрудником;
  - **Закрытые заявки** — вся история закрытых обращений;
  - переписка по заявке — тот же диалог, что видит сотрудник клиники;
  - кнопка **«Закрыть заявку»**, при закрытии — чекбокс «Дежурство в
    выходные»; если отмечен — счётчик часов (по умолчанию 1 ч, ±1 ч кнопками);
  - кнопка **«Открыть заявку»** — возвращает закрытую заявку в работу;
  - пункт меню (⋮) **«Отчёт по дежурствам»** — часы по каждому сотруднику
    IT-отдела, сгруппированные по неделям.

### IT-отдел (веб-приложение /it/, без Telegram)
Полный аналог Mini App выше (те же разделы, тот же чат, то же закрытие с
учётом дежурств и отчёт) — только вход по ФИО+паролю вместо Telegram, и
уведомления приходят как push в браузер вместо сообщений в бота. Оба
интерфейса работают с одними и теми же заявками одновременно — сотрудник
может, например, получить push на телефон, а ответить с компьютера через
Mini App, или наоборот. Подробности установки и подключения push — раздел 3.

## 5. Безопасность
- Пароли хранятся в виде bcrypt-хэшей.
- Папки `config/`, `includes/`, `db/` закрыты от прямого доступа через `.htaccess`.
- В папке `storage/uploads/` запрещено исполнение скриптов — только отдача файлов.
- Все запросы к БД выполняются через подготовленные выражения (PDO).
- Вебхук Telegram проверяет секретный токен (`TG_WEBHOOK_SECRET`).
- Подлинность данных из Mini App проверяется по HMAC-подписи Telegram (initData).
- Веб-приложение `/it/` использует ту же сессионную авторизацию, что и
  остальной сайт (пароль + обязательная смена при первом входе), и доступно
  только сотрудникам с ролью `it` в базе.
- Push-уведомления шифруются по стандарту Web Push (VAPID, ECDH P-256 +
  AES-128-GCM) — содержимое уведомления не видно ни push-сервису
  (Google/Mozilla/Apple), ни кому-либо ещё между сервером и браузером.

## 6. Обслуживание
- Резервные копии: делайте регулярный дамп базы данных (`mysqldump`) и
  архив папки `storage/uploads/`.
- Чтобы добавить новых сотрудников — просто добавьте строку в таблицу
  `users` (через phpMyAdmin) с паролем-хэшем (см. `db/seed_employees.sql`
  для примера) — по умолчанию используйте пароль `123456789`.
