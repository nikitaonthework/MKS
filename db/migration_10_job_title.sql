-- =====================================================================
--  Миграция 10: добавляет колонку users.job_title (должность) —
--  редактируется в /it/ (меню ⋮ → «Сотрудники») вместе с остальными
--  данными сотрудника, отображается в списке сотрудников и в шапке
--  заявки рядом с именем автора. Выполните один раз через phpMyAdmin
--  (вкладка «SQL»). При установке с нуля через актуальный db/schema.sql
--  это не нужно — колонка там уже есть.
-- =====================================================================

ALTER TABLE users ADD COLUMN job_title VARCHAR(150) NULL AFTER phone;
