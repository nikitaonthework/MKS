-- =====================================================================
--  Миграция 9: добавляет колонки users.phone и users.is_active — для
--  администрирования сотрудников из /it/ (изменение ФИО/телефона/пароля,
--  добавление и удаление, кнопка «Позвонить»). См. it/api/users_*.php.
--
--  is_active нужен для безопасного «удаления»: если у сотрудника уже
--  есть заявки/сообщения/часы дежурства в базе, удалить саму запись
--  нельзя, не потеряв эту историю (внешние ключи это и не позволят) —
--  вместо этого его просто деактивируют (is_active = 0): войти в систему
--  он больше не может, но история заявок сохраняется. Полностью удаляются
--  только записи без единой связанной заявки/сообщения.
--
--  Выполните один раз через phpMyAdmin (вкладка «SQL»). При установке
--  с нуля через актуальный db/schema.sql это не нужно — колонки там уже
--  есть.
-- =====================================================================

ALTER TABLE users ADD COLUMN phone VARCHAR(30) NULL AFTER full_name;
ALTER TABLE users ADD COLUMN is_active TINYINT(1) NOT NULL DEFAULT 1 AFTER role;
